{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the DeepFilesAI desktop shell. ADR 003 'capability allowlist': loopback HTTP to the sidecar only; no other outbound surface.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "name": "jarvis-sidecar",
          "sidecar": true,
          "args": true
        }
      ]
    }
  ]
}
