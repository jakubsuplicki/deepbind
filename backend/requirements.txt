fastapi==0.128.8
uvicorn[standard]==0.39.0
pydantic-settings==2.11.0
aiosqlite==0.22.1
anthropic==0.94.0
keyring==25.7.0
pyyaml==6.0.3
python-multipart==0.0.26
youtube-transcript-api==1.2.4
trafilatura==2.0.0
duckduckgo-search==7.5.1
markdownify==1.2.2
lxml_html_clean==0.4.4
pypdfium2==5.7.1  # PDF text extraction (Apache-2.0/BSD-3, ~9× faster than pdfplumber per ADR 013 knob-1)
defusedxml==0.7.1  # safe XML parsing for Jira imports (step 22a)
litellm==1.83.7
fastembed==0.8.0  # local embeddings for semantic search
psutil==7.2.2  # hardware detection for local model recommendations
cryptography==47.0.0  # Ed25519 sign/verify for ADR 006 offline license
spacy==3.8.14  # NER for entity extraction (Polish + English)
# spaCy NER model packages — pinned to direct wheel URLs so `pip install -r`
# is reproducible without hitting spaCy's compatibility-table API. Required
# at runtime by services/entity_extraction.py (spacy.load(...)) and bundled
# inside the desktop installer per ADR 003 §A. Version must match spacy 3.8.x.
https://github.com/explosion/spacy-models/releases/download/pl_core_news_sm-3.8.0/pl_core_news_sm-3.8.0-py3-none-any.whl
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
mcp==1.27.0  # Model Context Protocol SDK (FastMCP server, stdio transport)
# python-dotenv is transitively pinned by litellm to ==1.0.1 (vulnerable to
# CVE-2026-28684). We force-upgrade to 1.2.2 via backend/overrides.txt:
#   - CI/audit:  uv pip install --override overrides.txt -r requirements.txt
#   - Local dev: install-backend.mjs runs `pip install --upgrade -r overrides.txt`
#                as a second pass after the main install.
# litellm only uses load_dotenv() which is API-stable across 1.0 -> 1.2.

# testing
pytest==9.0.3
pytest-asyncio==1.3.0
anyio[trio]==4.12.1
pytest-anyio==0.0.0
httpx==0.28.1
