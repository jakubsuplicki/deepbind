fastapi==0.128.8
uvicorn[standard]==0.39.0
pydantic-settings==2.11.0
aiosqlite==0.22.1
keyring==25.7.0
pyyaml==6.0.3
python-multipart==0.0.26
youtube-transcript-api==1.2.4
trafilatura==2.0.0
markdownify==1.2.2
lxml_html_clean==0.4.4
pypdfium2==5.7.1  # PDF text extraction (Apache-2.0/BSD-3, ~9× faster than pdfplumber per ADR 013 knob-1)
defusedxml==0.7.1  # safe XML parsing for Jira imports (step 22a)
ollama==0.6.2  # official Ollama Python client (Apache-2.0). ADR 015 §B.
fastembed==0.8.0  # local embeddings for semantic search
psutil==7.2.2  # hardware detection for local model recommendations
cryptography==47.0.0  # Ed25519 sign/verify for ADR 006 offline license
spacy==3.8.14  # NER for entity extraction (Polish + English)
# spaCy NER model package — pinned to a direct wheel URL so `pip install -r`
# is reproducible without hitting spaCy's compatibility-table API. Required
# at runtime by services/entity_extraction.py (spacy.load(...)) and bundled
# inside the desktop installer per ADR 003 §A. Version must match spacy 3.8.x.
#
# Per ADR 018 (v1 ships English-only), we use spaCy's multilingual NER model
# (xx_ent_wiki_sm) selected for license + drop-in spaCy-compatibility, NOT for
# its incidental 9-language coverage. Trained on WikiNER (CC-BY 3.0); model
# weights MIT-licensed. Replaces the previous `pl_core_news_sm` (GPL-3.0 — see
# commercial-licensing-audit.md finding #1) AND `en_core_web_sm` (MIT but
# OntoNotes-trained — finding #3); the consolidation closes both findings in
# one swap. The model's multilingual coverage is incidental, untested, and
# unmarketed per ADR 018.
https://github.com/explosion/spacy-models/releases/download/xx_ent_wiki_sm-3.8.0/xx_ent_wiki_sm-3.8.0-py3-none-any.whl
mcp==1.27.0  # Model Context Protocol SDK (FastMCP server, stdio transport)

# testing
pytest==9.0.3
pytest-asyncio==1.3.0
anyio[trio]==4.12.1
pytest-anyio==0.0.0
httpx==0.28.1
