# Copy to .env and fill in real values. .env is gitignored.
#
# DEV ONLY — end users do NOT need this file.
# Keys are stored in the browser (localStorage/sessionStorage) and sent
# per-request. This env var is a local dev convenience fallback only.

# Anthropic API key — get one at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-api03-...
